{
  "name": "advanced-power-flow-card",
  "version": "0.1.0",
  "description": "Advanced Home Assistant power flow card with three PV strings, two batteries and dedicated consumers.",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "lit": "^3.3.3"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.2.2"
  }
}
