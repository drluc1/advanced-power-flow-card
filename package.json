{
  "name": "advanced-power-flow-card",
  "version": "0.2.0",
  "description": "Flexible Home Assistant power flow card with dynamic PV systems, batteries and consumers.",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "lit": "^3.3.3"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.2.2"
  }
}
